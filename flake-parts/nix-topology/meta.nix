{
  description = "Adds bindings for the `github:oddlama/nix-topology` project to generate graphs of your networks.";

  inputs = {
    nix-topology.url = "github:oddlama/nix-topology";
  };
  conflicts = [ ];
  extraTrustedPublicKeys = [ ];
  extraSubstituters = [ ];
}
